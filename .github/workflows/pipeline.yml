name: RepoSea — Hourly Pipeline

on:
  schedule:
    - cron: "0 * * * *"

  workflow_dispatch:
    inputs:
      env:
        description: "Environment (prod / dev)"
        required: false
        default: "prod"
        type: choice
        options: [prod, dev]
      reset:
        description: "Reset fleet — wipe all simulated state and start a fresh fleet before running"
        required: false
        default: false
        type: boolean

  push:
    branches: [main]
    paths:
      - "pipeline/**"
      - ".github/workflows/pipeline.yml"

permissions:
  contents: write
  pages:    write
  id-token: write

concurrency:
  group: reposea-git-write-${{ github.ref }}
  cancel-in-progress: false

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  # ── JOB 1: Run the Medallion Pipeline ──────────────────────────────────────
  pipeline:
    name: "Bronze → Silver → Gold → Excel"
    runs-on: ubuntu-latest
    timeout-minutes: 20

    outputs:
      critical_count:   ${{ steps.summary.outputs.critical_count }}
      total_exposure:   ${{ steps.summary.outputs.total_exposure }}
      run_ts:           ${{ steps.summary.outputs.run_ts }}
      dbt_docs_enabled: ${{ steps.check_gcp.outputs.enabled }}

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run test suite
        run: python -m pytest tests/ -q
        env:
          PYTHONPATH: ${{ github.workspace }}

      # Only runs when "reset" is ticked on a manual "Run workflow" — never
      # on the hourly schedule or on a push. This is the reset button:
      # Actions tab -> Run workflow -> tick "reset" -> Run workflow.
      - name: Reset fleet (manual only)
        if: ${{ inputs.reset == true }}
        run: python -m pipeline.reset --yes
        env:
          PYTHONPATH: ${{ github.workspace }}

      - name: Run pipeline
        run: python -m pipeline.run_pipeline --env ${{ inputs.env || 'prod' }}
        env:
          PYTHONPATH: ${{ github.workspace }}
          OPENWEATHER_API_KEY: ${{ secrets.OPENWEATHER_API_KEY }}
          # 2-week overdue escalation emails (pipeline/alerts.py, run as Stage 4).
          # Leave these secrets unset and alerts run in DRY RUN (logged, not sent).
          SMTP_HOST: ${{ secrets.SMTP_HOST }}
          SMTP_PORT: ${{ secrets.SMTP_PORT }}
          SMTP_USER: ${{ secrets.SMTP_USER }}
          SMTP_PASSWORD: ${{ secrets.SMTP_PASSWORD }}
          SMTP_FROM: ${{ secrets.SMTP_FROM }}
          DASHBOARD_URL: https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/

      - name: Generate Excel report
        run: python -m pipeline.export_excel
        env:
          PYTHONPATH: ${{ github.workspace }}

      - name: Generate DA narrative
        run: python -m analysis.generate_narrative
        env:
          PYTHONPATH: ${{ github.workspace }}

      # ── Optional warehouse layer (BigQuery + dbt) ───────────────────────────
      # Runs automatically once GCP_SA_KEY_B64 + BQ_PROJECT_ID secrets are set
      # (Settings -> Secrets and variables -> Actions). Until then, this whole
      # block is skipped and the run behaves exactly as it does today — same
      # "degrades gracefully" pattern the SMTP alert secrets already use.
      - name: Check optional warehouse secrets
        id: check_gcp
        run: |
          if [ -n "$GCP_SA_KEY_B64" ] && [ -n "$BQ_PROJECT_ID" ]; then
            echo "enabled=true" >> "$GITHUB_OUTPUT"
          else
            echo "enabled=false" >> "$GITHUB_OUTPUT"
          fi
        env:
          GCP_SA_KEY_B64: ${{ secrets.GCP_SA_KEY_B64 }}
          BQ_PROJECT_ID: ${{ secrets.BQ_PROJECT_ID }}

      - name: Decode GCP service account key
        if: steps.check_gcp.outputs.enabled == 'true'
        run: echo "$GCP_SA_KEY_B64" | base64 -d > /tmp/gcp-key.json
        env:
          GCP_SA_KEY_B64: ${{ secrets.GCP_SA_KEY_B64 }}

      - name: Install warehouse + dbt dependencies
        if: steps.check_gcp.outputs.enabled == 'true'
        run: |
          pip install -r warehouse/requirements-warehouse.txt
          pip install dbt-core dbt-bigquery

      - name: Load Gold data to BigQuery
        if: steps.check_gcp.outputs.enabled == 'true'
        run: >
          python -m warehouse.bigquery_load
          --project "$BQ_PROJECT_ID" --snapshot-history
        env:
          PYTHONPATH: ${{ github.workspace }}
          GOOGLE_APPLICATION_CREDENTIALS: /tmp/gcp-key.json
          BQ_PROJECT_ID: ${{ secrets.BQ_PROJECT_ID }}

      - name: dbt build (staging + marts + tests)
        if: steps.check_gcp.outputs.enabled == 'true'
        working-directory: dbt/reposea
        run: dbt build --target prod
        env:
          DBT_PROFILES_DIR: ${{ github.workspace }}/dbt/reposea
          GOOGLE_APPLICATION_CREDENTIALS: /tmp/gcp-key.json
          BQ_PROJECT_ID: ${{ secrets.BQ_PROJECT_ID }}

      - name: dbt docs generate
        if: steps.check_gcp.outputs.enabled == 'true'
        working-directory: dbt/reposea
        run: dbt docs generate --target prod
        env:
          DBT_PROFILES_DIR: ${{ github.workspace }}/dbt/reposea
          GOOGLE_APPLICATION_CREDENTIALS: /tmp/gcp-key.json
          BQ_PROJECT_ID: ${{ secrets.BQ_PROJECT_ID }}

      - name: Stage dbt docs for Pages
        if: steps.check_gcp.outputs.enabled == 'true'
        run: |
          mkdir -p dbt-docs-site
          cp dbt/reposea/target/index.html      dbt-docs-site/ 2>/dev/null || true
          cp dbt/reposea/target/manifest.json   dbt-docs-site/ 2>/dev/null || true
          cp dbt/reposea/target/catalog.json    dbt-docs-site/ 2>/dev/null || true
          cp dbt/reposea/target/run_results.json dbt-docs-site/ 2>/dev/null || true

      - name: Upload dbt docs artifact
        if: steps.check_gcp.outputs.enabled == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: dbt-docs
          path: dbt-docs-site/
          retention-days: 30

      - name: Clean up GCP key
        if: always() && steps.check_gcp.outputs.enabled == 'true'
        run: rm -f /tmp/gcp-key.json

      - name: Validate Gold outputs
        id: summary
        run: |
          python3 - << 'PYEOF'
          import json, os, sys
          try:
              s = json.load(open('data/gold/summary.json'))
              print(f"Containers : {s['total_containers']}")
              print(f"Critical   : {s['critical_count']}")
              print(f"Warning    : {s['warning_count']}")
              print(f"Exposure   : ${s['total_penalty_usd']:,.2f}")
              if s['total_containers'] == 0:
                  print("ERROR: Gold output is empty")
                  sys.exit(1)
              with open(os.environ['GITHUB_OUTPUT'], 'a') as f:
                  f.write(f"critical_count={s['critical_count']}\n")
                  f.write(f"total_exposure={s['total_penalty_usd']:.2f}\n")
                  f.write(f"run_ts={s['generated_at'][:16]}\n")
          except Exception as e:
              print(f"Validation failed: {e}")
              sys.exit(1)
          PYEOF

      - name: Commit Gold data + Excel
        run: |
          git config user.name  "RepoSea Bot"
          git config user.email "pipeline@reposea.bot"
          git add data/gold/
          git add data/exports/reposea_report.xlsx || true
          git add analysis/LANE_RELIABILITY_NARRATIVE.md || true
          git add data/state/container_history.json || true
          git add data/state/alerts_sent.json || true
          git add data/state/container_ledger.json || true
          git add data/state/completed_containers.json || true
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "chore(data): pipeline run $(date -u +'%Y-%m-%dT%H:%M:%SZ') [skip ci]"
            for i in 1 2 3; do
              git pull --rebase origin main && git push && break
              echo "Push attempt $i failed — retrying after rebase..."
              sleep $((i * 5))
            done
          fi

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: pipeline-output-${{ github.run_number }}
          path: |
            data/gold/
            data/exports/
          retention-days: 30

  # ── JOB 2: Deploy to GitHub Pages ──────────────────────────────────────────
  deploy-pages:
    name: "Deploy → GitHub Pages"
    runs-on: ubuntu-latest
    needs: pipeline
    if: github.ref == 'refs/heads/main'

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - uses: actions/checkout@v4
        with:
          ref: main

      - name: Build Pages bundle
        run: |
          mkdir -p _site/data/gold _site/data/exports
          cp dashboard/index.html                       _site/index.html
          cp dashboard/map.html                         _site/map.html
          cp data/gold/containers.json                  _site/data/gold/containers.json
          cp data/gold/summary.json                     _site/data/gold/summary.json
          [ -f data/gold/history.json ] && cp data/gold/history.json _site/data/gold/history.json || echo "No history yet"
          [ -f data/gold/market.json  ] && cp data/gold/market.json  _site/data/gold/market.json  || true
          [ -f data/exports/reposea_report.xlsx ] && cp data/exports/reposea_report.xlsx _site/data/exports/ || true
          echo "Bundle: $(du -sh _site | cut -f1)"
          ls -lh _site/data/gold/

      # Only present when the warehouse-layer secrets are set (see Step 6 of
      # docs/SETUP_GUIDE.html) — publishes at /dbt-docs/ alongside the dashboard.
      - name: Download dbt docs (if built)
        if: needs.pipeline.outputs.dbt_docs_enabled == 'true'
        uses: actions/download-artifact@v4
        with:
          name: dbt-docs
          path: _site/dbt-docs

      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4

  # ── JOB 3: Alert on critical containers ────────────────────────────────────
  alert:
    name: "Alert — Critical Check"
    runs-on: ubuntu-latest
    needs: pipeline
    if: always()
    steps:
      - name: Report
        run: |
          echo "Run    : ${{ needs.pipeline.outputs.run_ts }}"
          echo "Critical: ${{ needs.pipeline.outputs.critical_count }}"
          echo "Exposure: \$${{ needs.pipeline.outputs.total_exposure }}"